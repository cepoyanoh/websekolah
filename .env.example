# Konfigurasi Lingkungan untuk Aplikasi Web SD Negeri 40 Kecamatan Pontianak Utara

# Port untuk server (default: 3000)
PORT=3000

# URI koneksi ke MongoDB (gunakan MongoDB Atlas untuk deployment)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/nama_database?retryWrites=true&w=majority

# Secret key untuk JWT (gunakan string acak yang kuat untuk produksi)
JWT_SECRET=rahasia_jwt_yang_kuat_dan_acak

# Durasi expire token JWT (dalam jam, default: 24h)
JWT_EXPIRES_IN=24h